# دليل إنشاء حساب الأدمن

## الخطوات المطلوبة:

### 1. إعداد متغيرات البيئة

أنشئ ملف `.env.local` في المجلد الرئيسي للمشروع وأضف:

```env
VITE_SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

**كيفية الحصول على Service Role Key:**
1. افتح Supabase Dashboard
2. اذهب إلى Settings > API
3. انسخ `service_role` key (هذا المفتاح السري - لا تشاركه أبداً!)

### 2. تطبيق Migration (إذا لم تكن مطبقة)

تأكد من تطبيق migration التي تضيف دوال منح صلاحية الأدمن:

```bash
# إذا كنت تستخدم Supabase CLI
supabase migration up
```

أو يمكنك تطبيقها يدوياً من Supabase Dashboard > SQL Editor

### 3. تشغيل السكريبت

#### الطريقة الأولى (باستخدام npm script):
```bash
npm run create-admin
```

#### الطريقة الثانية (باستخدام Node.js مباشرة):
```bash
node scripts/create-admin-account.js
```

#### الطريقة الثالثة (مع بيانات مخصصة):
```bash
ADMIN_EMAIL=admin@example.com ADMIN_PASSWORD=MySecurePass123 ADMIN_NAME="مدير النظام" node scripts/create-admin-account.js
```

### 4. حفظ بيانات الدخول

بعد تشغيل السكريبت، سيتم عرض:
- 📧 البريد الإلكتروني
- 🔑 كلمة المرور
- 👤 الاسم

**احفظ هذه البيانات في مكان آمن!**

## ملاحظات مهمة:

- إذا كان المستخدم موجوداً بالفعل، سيتم فقط منحه صلاحية الأدمن
- إذا لم يكن موجوداً، سيتم إنشاء حساب جديد مع صلاحية الأدمن
- كلمة المرور يتم توليدها تلقائياً إذا لم تحددها
- البريد الإلكتروني يتم تأكيده تلقائياً

## استكشاف الأخطاء:

### خطأ: "Missing required environment variables"
- تأكد من وجود ملف `.env.local` مع المتغيرات المطلوبة

### خطأ: "function grant_admin_role does not exist"
- قم بتطبيق migration أولاً: `supabase migration up`

### خطأ: "User already exists"
- هذا ليس خطأ! السكريبت سيمنح المستخدم الموجود صلاحية الأدمن
